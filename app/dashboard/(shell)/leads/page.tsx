import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function LeadsPage() {
  return (
    <PlaceholderPage
      title="Demandes"
      description="Leads entrants depuis le site + prise de contact directe."
      nextSprint="Sprint 1 — Capture auto depuis /contact, liste filtrable, passage en devis."
    />
  );
}
