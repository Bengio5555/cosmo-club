import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function ClientsPage() {
  return (
    <PlaceholderPage
      title="Clients"
      description="Annuaire des clients récurrents consolidés depuis leads/devis/factures."
      nextSprint="Sprint 1 — Création auto depuis lead converti, vue fiche + historique devis/factures."
    />
  );
}
