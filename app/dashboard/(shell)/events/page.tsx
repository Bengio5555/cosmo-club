import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Événements"
      description="Calendrier des prestations, briefings, assignations staff et stock."
      nextSprint="Sprint 4 — Calendrier, fiche événement, lien devis/facture, assignations."
    />
  );
}
