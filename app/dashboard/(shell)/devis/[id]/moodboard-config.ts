// Shared between the client picker and the server save action so the
// cap doesn't drift between UI and persistence. Mirrored in the public
// plaquette page (`app/devis/[number]/page.tsx`).
export const MOODBOARD_MAX = 8;
